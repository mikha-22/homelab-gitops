apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Remove the resources section entirely since we don't need it

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: homelab-gitops
      environment: homelab
      component: opentelemetry-demo

namespace: otel-demo
