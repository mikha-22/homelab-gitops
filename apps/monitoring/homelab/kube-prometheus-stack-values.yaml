# FILE: apps/monitoring/homelab/kube-prometheus-stack-values.yaml

# ====================
# GLOBAL CONFIGURATION
# ====================
global:
  rbac:
    create: true
    pspEnabled: false

# ====================
# GRAFANA CONFIGURATION
# ====================
grafana:
  enabled: true
  
  admin:
    existingSecret: grafana-admin-credentials
    userKey: admin-user
    passwordKey: admin-password
  
  persistence:
    enabled: true
    storageClassName: "local-path"
    size: 10Gi
    accessModes:
      - ReadWriteOnce
  
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts: 
      - grafana.milenika.dev
    annotations:
      # FIX: Updated to the correct CNAME from your IaC project
      external-dns.alpha.kubernetes.io/target: "0051fa80-d5c3-4095-b44f-a8f6e77f404f.cfargotunnel.com"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
      traefik.ingress.kubernetes.io/router.tls: "true"
    tls:
      - secretName: grafana-tls
        hosts:
          - grafana.milenika.dev
  
  serviceMonitor:
    enabled: true
    labels:
      release: kube-prometheus-stack
  
  additionalDataSources:
    - name: Loki
      type: loki
      url: http://loki-gateway.monitoring.svc.cluster.local
      access: proxy
      isDefault: false
      jsonData:
        timeout: 60
        maxLines: 1000
    
    - name: Prometheus
      type: prometheus
      url: http://kube-prometheus-stack-prometheus.monitoring.svc.cluster.local:9090
      access: proxy
      isDefault: true
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi

# ====================
# PROMETHEUS CONFIGURATION
# ====================
prometheus:
  enabled: true
  
  service:
    type: ClusterIP
    port: 9090
  
  serviceMonitor:
    enabled: true
    
  prometheusSpec:
    retention: 30d
    retentionSize: 45GB
    
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: "local-path"
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi
    
    resources:
      limits:
        cpu: 1000m
        memory: 2Gi
      requests:
        cpu: 500m
        memory: 1Gi
    
    securityContext:
      runAsNonRoot: true
      runAsUser: 65534
      fsGroup: 65534
    
    additionalScrapeConfigs: []
    
    ruleSelector: {}
    ruleNamespaceSelector: {}
    
    serviceMonitorSelector: {}
    serviceMonitorNamespaceSelector: {}
    
    podMonitorSelector: {}
    podMonitorNamespaceSelector: {}

# ====================
# ALERTMANAGER CONFIGURATION
# ====================
alertmanager:
  enabled: false
  
# ====================
# PROMETHEUS OPERATOR CONFIGURATION
# ====================
prometheusOperator:
  enabled: true
  
  resources:
    limits:
      cpu: 200m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 100Mi
  
  admissionWebhooks:
    enabled: true
    patch:
      enabled: true

# ====================
# KUBE-STATE-METRICS CONFIGURATION
# ====================
kube-state-metrics:
  enabled: true
  
# ====================
# PROMETHEUS NODE EXPORTER CONFIGURATION
# ====================
prometheus-node-exporter:
  enabled: true
  
  prometheus:
    monitor:
      enabled: true
      
# ====================
# GRAFANA OPERATOR (if needed)
# ====================
grafana-operator:
  enabled: false

# ====================
# CLEANUP CONFIGURATION
# ====================
cleanupCustomResourceBeforeInstall: false
