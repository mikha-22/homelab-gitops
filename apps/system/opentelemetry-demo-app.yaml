# apps/system/opentelemetry-demo-app.yaml - FIXED
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opentelemetry-demo
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io  # FIXED: was "argotroj.io"
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: default
  sources:
    # Source 1: The Helm chart
    - repoURL: https://open-telemetry.github.io/opentelemetry-helm-charts
      targetRevision: 0.37.2
      chart: opentelemetry-demo
      helm:
        releaseName: otel-demo
        valueFiles:
          - $values/apps/opentelemetry-demo/values.yaml
    # Source 2: Your Git repository with values
    - repoURL: https://github.com/mikha-22/homelab-gitops
      targetRevision: HEAD
      ref: values
      
  destination:
    server: https://kubernetes.default.svc
    namespace: otel-demo
    
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 5m
        
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
